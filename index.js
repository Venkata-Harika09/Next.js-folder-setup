import React from 'react';
import ReactDOM from 'react-dom/client';
import WYSIWYGBlogEditor from './blog';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WYSIWYGBlogEditor/>
  </React.StrictMode>
);
